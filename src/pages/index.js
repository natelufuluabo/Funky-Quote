import Layout from "../../components/layout";
import BackgroundPicker from "../../components/backgroundPicker";

const Home = () => {
    return (
        <Layout home>
            <BackgroundPicker />
        </Layout>
    )
};

export default Home;